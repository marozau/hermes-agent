---
spec:
  persona: PM
  phase: planning
  imperative_preamble: true
  verification:
    - "PRD updated in planning-artifacts/"
    - "Changes are traceable"
    - "No conflicting requirements introduced"
---

Edit and refine the existing PRD based on user feedback:

1. Read the current PRD from `planning-artifacts/` (find the latest `prd-*.md`)
2. Understand the requested changes
3. Apply targeted edits — add, remove, or modify sections
4. Validate that edits maintain internal consistency
5. Update the document's `last_updated` field

For major rework, consider creating a new version with a new date in the filename.

Run test automation workflow:

1. **Load Context** — Review existing tests, framework, and coverage gaps
2. **Generate Unit Tests** — Create unit tests for core logic
3. **Sub-agent: API Tests** — Generate API-level integration tests
4. **Sub-agent: Backend Tests** — Generate backend/service tests
5. **Valida...[truncated]
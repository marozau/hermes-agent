Run Non-Functional Requirements testing:

1. **Load Context** — Review NFR criteria from PRD and architecture
2. **Define Thresholds** — Set pass/fail thresholds for each NFR dimension
3. **Sub-agent Security** — Run security assessment (4-child fan-out)
4. **Sub-agent Performance** — Run performance benchmark
5. **Sub-age...[truncated]
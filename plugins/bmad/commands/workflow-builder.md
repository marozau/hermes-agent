---
spec:
  persona: Builder
  phase: solutioning
  imperative_preamble: true
  verification:
    - "Workflow designed"
    - "Steps defined"
    - "Integrity checks pass"
---

Design and build BMAD workflows:

1. **Design** — Workflow requirements, steps, transitions
2. **Scaffold** — Generate workflow structure from template
3. **Quality Scan** — Run integrity, prompt, and execution checks
4. **Iterate** — Fix findings, re-scan
5. **Output** — Production-ready BMAD workflow

Includes: quality-scan-* analyzers, references, build process.
